services:
  zotdav:
    container_name: zotdav
    build: .
    restart: always
    env_file:
      - .env
    # ports:
    #   - '8080:8080'
    volumes:
      - '/mnt/storage/zotdav:/var/www/webdav'
    networks:
      - proxyman_default

networks:
  proxyman_default:
    external: true
